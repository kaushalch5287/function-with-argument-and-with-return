#include<stdio.h>
float divide(float a,float b)
{
  float  c;
  c = a / b;
  return c;
}

int main ()
{
 float x, y, z;
  scanf ("%f%f", &x, &y);
  z = divide (x, y);
  printf ("%f", z);
  return 0;
}
